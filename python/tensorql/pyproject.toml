[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "tensorql"
version = "0.1.0"
description = "Tiny PyTorch reader for skibidi-ql training snapshots"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
